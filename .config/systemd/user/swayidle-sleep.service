[Unit]
Description=Swayidle sleep daemon

[Service]
ExecStart=/usr/bin/env swayidle \
    timeout 2700 systemctl suspend \
    before-sleep systemctl --user start lock
Restart=always

[Install]
WantedBy=default.target
